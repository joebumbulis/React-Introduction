import React from 'react'
import BlogComponent from './blog_component'

export default React.createClass({
  render: function () {
    return (
      (<main>
        <BlogComponent/>
      </main>)
    );
  }
});
